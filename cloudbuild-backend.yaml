steps:
- name: 'gcr.io/cloud-builders/docker'
  args: ['build', '-t', '$_REGION-docker.pkg.dev/$PROJECT_ID/silvia-repo/silvia-backend:latest', '-f', 'backend/Dockerfile', '.']
images:
- '$_REGION-docker.pkg.dev/$PROJECT_ID/silvia-repo/silvia-backend:latest'
